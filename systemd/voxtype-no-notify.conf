# Prepends the notify-send shim directory to intercept voxtype's
# hardcoded notify-send calls. Note: this replaces PATH for the entire
# voxtype.service — adjust if voxtype needs additional custom paths.
[Service]
Environment=PATH=%h/.local/lib/voxtype-shims:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:%h/.local/bin:%h/.cargo/bin
